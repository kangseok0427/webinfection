class Ship { constructor(x, y) { this.x = x; this.y = y; this.angle = 0; this.velocityX = 0; this.velocityY = 0; this.hp = 100; } update() { this.x += this.velocityX; this.y += this.velocityY; if (this.x < 0) this.x = 800; if (this.x > 800) this.x = 0; if (this.y < 0) this.y = 600; if (this.y > 600) this.y = 0; } draw(ctx) { ctx.save(); ctx.translate(this.x, this.y); ctx.rotate(this.angle); ctx.beginPath(); ctx.moveTo(0, -10); ctx.lineTo(-10, 10); ctx.lineTo(10, 10); ctx.closePath(); ctx.fillStyle = 'white'; ctx.fill(); ctx.restore(); } } class Bullet { constructor(x, y, angle) { this.x = x; this.y = y; this.angle = angle; this.velocityX = Math.cos(angle) * 5; this.velocityY = Math.sin(angle) * 5; } update() { this.x += this.velocityX; this.y += this.velocityY; } draw(ctx) { ctx.beginPath(); ctx.arc(this.x, this.y, 2, 0, Math.PI * 2); ctx.fillStyle = 'white'; ctx.fill(); } } let ship = new Ship(400, 300); let bullets = []; let canvas = document.getElementById('game-canvas'); let ctx = canvas.getContext('2d'); let gameOver = false; function draw() { ctx.fillStyle = 'black'; ctx.fillRect(0, 0, 800, 600); ship.draw(ctx); for (let bullet of bullets) { bullet.draw(ctx); } ctx.font = '24px Arial'; ctx.fillStyle = 'white'; ctx.textAlign = 'left'; ctx.textBaseline = 'top'; ctx.fillText(`HP: ${ship.hp}`, 10, 10); if (gameOver) { ctx.font = '48px Arial'; ctx.fillStyle = 'white'; ctx.textAlign = 'center'; ctx.textBaseline = 'middle'; ctx.fillText('Game Over', 400, 300); } } function update() { if (!gameOver) { ship.update(); for (let i = bullets.length - 1; i >= 0; i--) { bullets[i].update(); if (bullets[i].x < 0 || bullets[i].x > 800 || bullets[i].y < 0 || bullets[i].y > 600) { bullets.splice(i, 1); } } } } function loop() { update(); draw(); if (!gameOver) { requestAnimationFrame(loop); } } document.addEventListener('keydown', (e) => { if (e.key === 'w') { ship.velocityX += Math.cos(ship.angle) * 0.1; ship.velocityY += Math.sin(ship.angle) * 0.1; } if (e.key === 'a') { ship.angle -= 0.1; } if (e.key === 'd') { ship.angle += 0.1; } if (e.key === ' ') { if (!gameOver) { bullets.push(new Bullet(ship.x, ship.y, ship.angle)); ship.hp -= 1; if (ship.hp <= 0) { gameOver = true; } } } }); loop();